export const metadata = {
    title: 'Next.js',
    description: "fdfdf",
};

export default function DashboardLayout({ children }: {children: React.ReactNode} ) {
    return (
        <html lang="en">
        fddfdf
        <body> {children} </body>
        </html>
    );
}